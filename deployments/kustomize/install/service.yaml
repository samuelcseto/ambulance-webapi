kind: Service
apiVersion: v1
metadata:
    name: scs-ambulance-webapi
spec:
    selector:
        pod: scs-ambulance-webapi-label
    ports:
        - name: http
          protocol: TCP
          port: 80
          targetPort: webapi-port
